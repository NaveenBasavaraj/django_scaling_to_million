## Django Scaling To Million

This repository contains learning projects focused on Django, scaling concepts, and backend engineering.

### Available Project

- `project 1 - ORM playground`: Dockerized Django + DRF + PostgreSQL + Redis + React project for practicing ORM queries using read-only APIs.

Start here:

- `cd "project 1 - ORM playground"`
- See the project README for setup and phase-wise learning plan.
